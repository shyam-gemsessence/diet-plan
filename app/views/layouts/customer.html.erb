<!DOCTYPE html>
<html>
<head>
  <title>Fresh Basket - Customer</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
</head>
<body>

  <% flash.each do |message| %>
      <p><%= message %></p>
  <% end %>

 
  <%= render 'shared/customer_header' %>

  <main class="customer-main-content">
    <%= yield %>
  </main>

</body>
</html>
